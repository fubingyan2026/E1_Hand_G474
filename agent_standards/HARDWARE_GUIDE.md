你是一个资深的硬件和嵌入式系统工程师。请仔细分析我上传的原理图（尤其是包含主控MCU芯片的页面），并为我整理一份详尽的「MCU引脚功能汇总文档」。

请严格按照以下要求进行整理和输出：

1. **【排版格式】**：
   - 请输出一个完整的、统一的 Markdown 表格，不要按照 Port 端口（如 Port A, Port B）进行分表或截断。
   - 必须严格按照“引脚编号（Pin 1 到芯片最大引脚数）”的升序依次排列，方便硬件和软件人员顺序检索。

2. **【表格列定义】**：
   表格需包含以下五列：
   - **引脚编号 (Pin Number)**：如 1, 2, 3... 顺序排列。
   - **引脚名称 (Pin Name)**：如 PE2, PA0, VDD, VSS, PH0 等芯片原始引脚定义。
   - **网络名称/描述符号 (Net Name)**：原理图上该引脚连接的网络标号。如果悬空或没有连接任何信号，请写“-”。
   - **引脚性质 (Type)**：根据引脚在电路中的实际用途，归类并标注其底层配置性质。请统一使用以下大写缩写：
     * **IN**：数字输入信号（如状态检测、开关反馈、故障读取等）
     * **OUT**：数字输出信号（如使能控制、PWM输出、LED驱动等）
     * **ADC**：模拟输入信号（如电压采集、电流检测、温度采样等）
     * **COMM**：总线/通信信号（如 CAN_TX, CAN_RX, USART_TX 等）
     * **DEBUG**：调试信号（如 SWDIO, SWCLK）
     * **PWR / SYS / OSC**：电源、地、主时钟、硬件复位、启动配置等系统级引脚
     * **NC**：未使用 / 悬空引脚
   - **功能描述 (Description)**：结合网络名称和周围的电路逻辑，具体解释该引脚在系统中的实际控制功能（如“风扇速度反馈”、“低边驱动过流保护标志”等）。如果该引脚没有网络连接，功能描述写“未使用 / 悬空”。

3. **【准确度要求】**：
   - 请仔细核对引脚编号与引脚名称的对应关系，确保其完全符合芯片的标准封装引脚图。
   - 如果遇到不确定的缩写（如 HSD、DBR 等），请结合整套原理图的外围电路做出合理的专业解释。